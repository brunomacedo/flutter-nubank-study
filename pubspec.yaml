name: "uolplay"
description: Aplicativo de filmes e vídeos do UOL.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.7.4
  cupertino_icons: ^0.1.3
  flutter_native_splash: ^0.1.9

flutter_native_splash:
  image: images/splash3x.png
  color: "000000"

flutter_icons:
  image_path: "images/icon.png"
  android: true
  ios: true

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - images/
